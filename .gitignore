# Export archives
pal-export-*.zip

# Automatic backups
backups/

node_modules/

.test-tmp/
.test-home/

# Eval runner logs (promptfoo writes error/debug logs to cwd)
eval/logs/
.test-install-home/

# macOS debris
.DS_Store

# consulting-report — demo render artifacts (regenerated by generate-pdf.ts)
assets/skills/consulting-report/demo/*.pdf
assets/skills/consulting-report/demo/*.html
assets/skills/consulting-report/demo/diagrams-compressed/

# Skill tools compiled to .mjs for Node (scripts/build-skill-tools.ts). Generated at
# `prepack` so the published tarball ships them; never committed. See the script header.
assets/skills/*/tools/*.mjs

# Mutation testing sandbox + reports
.stryker-tmp/
stryker.log
reports/
